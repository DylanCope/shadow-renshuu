# URL of the deployed backend (Railway).
# Leave empty for local dev — Vite proxy handles /api automatically.
VITE_API_URL=https://your-backend.railway.app

# Set to "false" to hide the Ollama (local) provider option in the UI.
# Useful for production deployments where Ollama is not reachable.
VITE_SHOW_OLLAMA=false
