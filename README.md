# Shadow Renshuu

Shadow Renshuu is an interactive learning tool designed to help users practice and refine their skills in various subjects.

## Features
- Sign-in + persistent sessions
- Adaptive content delivery
- Progress tracking

## Configuration

Firebase is used for auth + session metadata storage (Firestore), while deployment remains Vercel (frontend) and Railway (backend).